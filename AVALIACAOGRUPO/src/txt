salario
peso,altura/ media de peso  
gasto de combustivel


CALC SALARIO

Double horaT = Double.parseDouble(JOptionPane.showInputDialog("HORAS TRABALADAS "));
        Double VHora = Double.parseDouble(JOptionPane.showInputDialog("DIGITE VALOR DA HORA"));

        Double salarioBR = horaT * horaT;
        Double salario = VHora * horaT;
        Double inss = salario * 0.08;
        Double sindicato = salario * 0.05;
        Double impostoR = salario * 0.11;
        salario = (salario * 11) / 100;
        salario = (salario * 8) / 100;
        salario = (salario * 5) / 100;
        JOptionPane.showMessageDialog(null,"salario liquido" +salario+" \n ");




        JButton  C = new JButton("C");
        JButton  p = new JButton("()");
        JButton  por = new JButton("%");
        JButton  div = new JButton("\u00f7");
        JButton  sete= new JButton("7");
        JButton  oit = new JButton("8");
        JButton  nov = new JButton("9");
        JButton  mult = new JButton("*");
        JButton  qua = new JButton("4");
        JButton  cin = new JButton("5");
        JButton  se = new JButton("6");
        JButton  men = new JButton("-");
        JButton  um = new JButton("1");
        JButton  dois = new JButton("2");
        JButton  tre = new JButton("3");
        JButton  mais = new JButton("+");
        JButton  maimen = new JButton("+/-");
        JButton  zero = new JButton("0");
        JButton vir = new JButton(",");
        JButton  igual = new JButton("=");
  
        import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JPanel;
import javax.swing.JTextField;
import javax.swing.SwingUtilities;
import java.awt.FlowLayout;
import java.awt.Font;
import java.awt.GridBagConstraints;
import java.awt.GridBagLayout;
import java.awt.GridLayout;
import java.awt.Insets;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

public class Calculadora3 extends JPanel {
    private JTextField textFieldHora;
    private JTextField textFieldMinuto;
    private JTextField textFieldSegundo;
    private int conversaoAtual = 0; // 0 = horas, 1 = minutos, 2 = segundos

    public Calculadora3() {
        super();

        JLabel titleLabel = new JLabel("Conversor de Tempo");
        titleLabel.setFont(new Font("Arial", Font.BOLD, 24));

        this.setLayout(new GridBagLayout());
        GridBagConstraints gbc = new GridBagConstraints();
        gbc.insets = new Insets(30, 50, 30, 50);

        gbc.gridx = 0;
        gbc.gridy = 0;
        gbc.gridwidth = 2;
        this.add(titleLabel, gbc);

        JPanel panel = new JPanel(new GridLayout(3, 2));

        textFieldHora = new JTextField();
        textFieldMinuto = new JTextField();
        textFieldSegundo = new JTextField();
        textFieldSegundo.setEditable(true);

        panel.add(new JLabel("Horas:"));
        panel.add(textFieldHora);
        panel.add(new JLabel("Minutos:"));
        panel.add(textFieldMinuto);
        panel.add(new JLabel("Segundos:"));
        panel.add(textFieldSegundo);

        gbc.gridx = 0;
        gbc.gridy = 1;
        gbc.gridwidth = 4;
        this.add(panel, gbc);

        JPanel buttonPanel = new JPanel(new FlowLayout());

        JButton converterButton = new JButton("Converter");
        converterButton.addActionListener(new ActionListener() {
            public void actionPerformed(ActionEvent e) {
                realizarConversao();
            }
        });

        buttonPanel.add(converterButton);

        gbc.gridx = 0;
        gbc.gridy = 2;
        gbc.gridwidth = 2;
        this.add(buttonPanel, gbc);
    }

    private void realizarConversao() {
        try {
            int horas = 0;
            int minutos = 0;
            int segundos = 0;

            switch (conversaoAtual) {
                case 0: // Converter para minutos
                    horas = Integer.parseInt(textFieldHora.getText());
                    minutos = horas * 60;
                    segundos = horas * 3600;
                    textFieldMinuto.setText(String.valueOf(minutos));
                    textFieldSegundo.setText(String.valueOf(segundos));
                    conversaoAtual = 1;
                    break;
                case 1: // Converter para segundos
                    minutos = Integer.parseInt(textFieldMinuto.getText());
                    horas = minutos / 60;
                    segundos = minutos * 60;
                    textFieldHora.setText(String.valueOf(horas));
                    textFieldSegundo.setText(String.valueOf(segundos));
                    conversaoAtual = 2;
                    break;
                case 2: // Converter para horas
                    segundos = Integer.parseInt(textFieldSegundo.getText());
                    horas = segundos / 3600;
                    minutos = (segundos % 3600) / 60;
                    textFieldHora.setText(String.valueOf(horas));
                    textFieldMinuto.setText(String.valueOf(minutos));
                    conversaoAtual = 0;
                    break;
            }
        } catch (NumberFormatException ex) {
            textFieldMinuto.setText("Erro");
            textFieldSegundo.setText("Erro");
        }
    }

    public static void main(String[] args) {
        SwingUtilities.invokeLater(new Runnable() {
            public void run() {
                JFrame frame = new JFrame("Calculadora3");
                frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
                frame.add(new Calculadora3());
                frame.pack();
                frame.setVisible(true);
            }
        });
    }
}
